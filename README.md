# Realtime Game Framework


## Install

* `npm install`
* `tsd install`

## Build

* `tsc -p . [-w]` in root
* Make sure that `data/db` directory exists

## Start

* `mongod.exe --dbpath data/db`
* `node app`
